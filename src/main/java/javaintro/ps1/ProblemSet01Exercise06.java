package javaintro.ps1;

public class ProblemSet01Exercise06 {

    /*
     * This method creates two variables
     * that are assigned Strings. The method is
     * supposed to return the firstName variable.
     * Fix the problem so it returns the firstName.
     */
    public static String getFirstName() {
        String firstName = "George";
        String lastName = "Washington";
        return lastName;
    }
}
