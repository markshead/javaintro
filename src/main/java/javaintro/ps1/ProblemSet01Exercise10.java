package javaintro.ps1;

public class ProblemSet01Exercise10 {
    
    /*
     * This method takes an argument or input.
     * This input is an int and can be used as
     * part of what the method does.
     * 
     * In this case, the method is supposed to return
     * the square of input or input * input. Currently
     * it returns the cube. Change the method to work
     * correctly.
     */
    public static int squareInput(int input) {
        return input * input * input;
    }

}
