package javaintro.ps02;

/*
 * In these exercises we are going to use some of the primitives we've learned about.
 */
public class ProblemSet02Exercise08 {
    
    /*
     * Test to see if the char used as an input is equal to 'X' return true if it is
     * and false otherwise.
     * 
     * Right now the if statement always returns true.  Change it to test:
     *  if ('X' == inputChar) {
     *  Notice the double equals sign.  A single equals sign would try to assign the value of inputChar to
     *  'X' which of course wouldn't work.  The double equals sign tests for equality and returns true if two
     *  things are equal and false otherwise.
     */
    public static boolean isCharX(char inputChar) {
        boolean returnValue = false;
        
        //You will need to modify this if statement as shown in the comment
        //above the method.
        if(true) {
            returnValue = true;
        }
        
        return returnValue;
    }
    
    /*
     * Test to see if a float is less than zero.  Return true if it is less than zero and false otherwise.
     */
    
    public static boolean floatIsLessThanZero(float x) {
        boolean returnValue = false;
        
        //This statement always evaluates as true.  Change it to check to see if
        //x is less than 0.  if(x < 0)
        if(true) {
            returnValue = true;
        }
        
        return returnValue;
    }
    
    
       
    

     
}
