package javaintro.ps02;

/*
 * These methods are the basic arithmetic for
 * a calculator.  Right now they all return 0.
 * Complete each method so it returns the correct 
 * value based on the operation it indicates.
 * 
 * Notice that each method names its argument
 * variables the same thing.  There is no confusion
 * because the variables are only "in scope" in 
 * that method and no where else.
 */
public class ProblemSet02Exercise01 {
    

    /*
     * Add the first and second numbers.
     */
    public static int addition(int firstNumber, int secondNumber) {
        return 0;
    }
    
    /*
     * Subtract the second number from the first number. So,
     * firstNumber - secondNumber.
     */
    public static int subtraction(int firstNumber, int secondNumber) {
        return 0;
    }
    
    /*
     * Multiply the two numbers together.
     */
    public static int multiplication(int firstNumber, int secondNumber) {
        return 0;
    }
    
    /*
     * Divide the first number by the second number. So
     * firstNumber / secondNumber
     */
    public static int division(int firstNumber, int secondNumber) {
        return 0;
    }
    
    

}
