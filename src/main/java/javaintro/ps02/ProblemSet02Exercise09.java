package javaintro.ps02;

/*
 * In these exercises we are going to use some of the primitives we've learned about.
 */
public class ProblemSet02Exercise09 {
    
    /*
     * return true if the input byte is less then 50 and false otherwise. The only thing the method
     * currently does is return false, so you'll have to implement everything on your own
     * using what you've learned so far.
     */
    public static boolean isLessThanFifty(byte inputByte) {
        return false;
    }
}
