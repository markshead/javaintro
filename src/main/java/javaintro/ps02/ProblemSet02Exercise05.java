package javaintro.ps02;

/*
 * Primitives are data types like int and boolean. In this exercise we are going to look at ints, shorts and byte primitives.
 * You can read more about primitives here:
 * http://download.oracle.com/javase/tutorial/java/nutsandbolts/datatypes.html
 */
public class ProblemSet02Exercise05 {
    
    /*
     * We have already encountered the int primitive. ints can hold values from
     *  -2,147,483,648 to 2,147,483,647.  In this exercise, set the variable smallestInt
     *  to the lowest possible value for an int.  Note: Don't include commas when you set 
     *  the value.  
     */
    public static int getSmallestInt() {
        int smallestInt = 0;
        return smallestInt;
    }
    
    /*
     * Similiar to above, but this time set it to the highest value an int can hold.
     */
    public static int getLargestInt() {
        int largestInt = 0;
        return largestInt;
    }
    
    /*
     * A primitive of type short is similiar to an int, but it can only hold much smaller numbers. A short
     * can hold values from -32,768 to 32,767.  
     */
    public static short getSmallestShort() {
        short smallestShort = 0;
        return smallestShort;
    }
    
    public static short getLargestShort() {
        short largestShort = 0;
        return largestShort;
    }


    /* 
     * A primitive of type byte can only hold even smaller numbers.  The range of a byte can be from -128 to 127.
    */
    public static byte getSmallestByte() {
        byte smallestByte = 0;
        return smallestByte;
    }
    
    public static byte getLargestByte() {
        byte largestByte = 0;
        return largestByte;
    }
    
     
}
