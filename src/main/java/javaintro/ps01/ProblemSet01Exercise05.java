package javaintro.ps01;

public class ProblemSet01Exercise05 {
    
    
    /*
     * This method creates two variables x and y. 
     * A variable is a storage location.  The int
     * 5 has been assigned to x and the int 10 has been
     * assigned to y.
     * 
     * Right now the method returns the value of y or 10.
     * Change it so it returns x instead.  That way it 
     * will return 5.
     */
    public static int getVariableX() {
        int x = 5;
        int y = 10;
        return y;
    }

}
